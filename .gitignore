# Spot-VM measurement scratch that the campaign regenerates
data/*.stdout
launchd.out.log
launchd.err.log
.cycle.lock/

# Banked HLO and profiler archives: hundreds of MiB, kept locally as the durable record
artifacts/
