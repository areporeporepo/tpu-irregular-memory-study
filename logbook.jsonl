{"at": "2026-08-16T11:06:46+00:00", "kind": "event", "text": "Campaign started. v6e quota is 512 on-demand and 1536 spot, but a nine-zone sweep found multi-chip capacity in only two zones: us-east1-d and asia-northeast1-b.", "detail": "us-east5-a/b/c, us-south1-a and us-central1-a all refuse v6e-4 and v6e-8 with 'no more capacity'. Single chips are available almost everywhere. Quota is not capacity."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "finding", "text": "A TensorCore row gather of 1M indices from a 4.2M-row table runs at 1.3 GB/s, which is 0.08% of the chip's 1,600 GB/s of HBM bandwidth.", "detail": "This is the headroom the whole study is about. Gemini's read: HBM transaction granularity means a random small-row read pulls a wide line and discards most of it, predicting sorted or block-contiguous indices could recover roughly 10x. Untested."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "correction", "text": "The first fabric numbers were my own harness, not the fabric. A 0.55 ms floor appeared identically for two different collectives at four payload sizes, which is the signature of host dispatch.", "detail": "Fixed by chaining 32 collectives inside one jit and dividing. Corrected all_to_all at 0.25 MiB: 0.027 ms of fabric under 0.53 ms of dispatch. The uncorrected figure was 20x too high."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "finding", "text": "Host dispatch costs 0.51 ms at 0.25 MiB rising to 1.23 ms at 16 MiB, up to 20x the cost of the collective itself.", "detail": "Scale-independent and actionable: small collectives on TPU are orchestration-bound rather than network-bound. This is what an on-chip collectives engine like TPU 8i's CAE exists to remove."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "finding", "text": "all_to_all at 16 MiB per chip is free from 8 to 16 chips (+1%) and then costs +77% at 32 chips: 0.236, 0.239, 0.423 ms.", "detail": "Measured on a v6e-32 in us-east1-d, median of 20 samples, dispatch amortised."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "correction", "text": "My host-boundary explanation for the +77% is probably wrong. Gemini proposes bisection congestion, which fits better.", "detail": "16 chips is likely a 4x4 torus and 32 a 4x8. The bisection cut stays 4 links wide while injected data doubles, so the same physical bottleneck carries twice the traffic. Test: run a 16-chip sub-communicator on the 32-chip slice at identical payload. If it stays fast, the effect is scaling and congestion rather than slice shape."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "finding", "text": "A collective costs 1.37x to 1.62x more while the MXU is busy: 1.62x at 8 chips, 1.61x at 16, 1.37x at 32.", "detail": "This is the number that matters to anyone running thousands of chips, because step-time models are built from idle microbenchmarks. Tail was tight, p99/p50 = 1.00, on a single-tenant slice."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "decision", "text": "Burst a v6e-256 once for the scaling curve instead of running a larger steady fleet, and delete it from a shell trap.", "detail": "A 256-chip slice costs about $359/hour on spot. A third point on the curve is the most valuable data available to us, and leaving the slice running is the most expensive mistake available, so the delete is unconditional."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "plan", "text": "Reframed from 'benchmark SparseCore' to 'where does irregular work belong, in a local gather engine or on the wire'.", "detail": "Gemini's objection: TPU 8i deletes four SparseCores and adds a collectives engine, so SparseCore looks like a deprecating branch. Half right. 8t keeps SparseCore and v7x has it, so the unit is being split by role: training keeps local gather, inference moves it to the fabric. That makes the boundary itself the research question."}
{"at": "2026-08-16T11:06:47+00:00", "kind": "event", "text": "Methodology, recorded deliberately. This campaign is run by Claude Code on Opus 5 with a 1M context, using background tasks for concurrent TPU jobs, launchd for a 20-minute measurement cycle, and Gemini 3.1 Pro as an adversarial advisor on framing and confounds.", "detail": "Gemini has changed the direction twice: it identified SparseCore as a possibly deprecating branch, and it replaced my host-boundary explanation with bisection congestion. Both are logged as corrections. This is the same shape as Discovery Loop, the public benefit corporation Jeff Dean, Sanjay Ghemawat, Oriol Vinyals and Quoc Le launched on 5 August 2026 to automate the propose-run-evaluate loop, with hardware design named as a target domain. The notable part is that the loop is now cheap enough for one student to run."}
